{
  "name": "canary-rollout-controller",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/target-service",
    "packages/controller",
    "packages/validation"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
